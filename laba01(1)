#include<stdio.h>
#include<math.h>

int main()
{   
    float a = 1000, b = 0.0001, result;
    
    result = (pow(a+b, 2) - (pow(a,2) + 2*a*b)) / pow(b,2);
    printf("%f\n result = ", result);
    
    return 0;
}
